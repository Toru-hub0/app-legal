# 一時ファイル（Play Console へのアップロード用）

Play Console のアセット欄はネイティブのファイル選択ダイアログを開くため、
自動化からは直接ファイルを渡せない。ブラウザのドロップ経由で入れるために、
一時的にここへ置いて HTTPS で取得している（GitHub Pages は CORS 許可を返す）。

**アップロードが済んだら削除する。** 画像自体は Play ストアで公開されるものなので
秘密ではないが、法務ページのリポジトリに置いたままにしない。
